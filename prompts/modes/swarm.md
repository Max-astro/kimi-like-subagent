## Swarm mode

The user has enabled swarm-oriented decomposition. Do a small amount of exploration if needed, then divide the work into distinct non-conflicting items and use AgentSwarm. Prefer a useful number of narrow tasks over an arbitrary target count. Do not assign concurrent writes to the same working directory. If the task is not meaningfully parallelizable, explain that briefly and proceed without manufacturing a swarm.
