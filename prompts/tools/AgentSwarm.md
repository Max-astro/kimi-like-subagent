Launch item-based subagents from one shared template, resume existing agents, or combine both. New tasks require `prompt_template` containing the exact `{{item}}` placeholder and distinct `items`; at least two items are required unless `resume_agent_ids` is supplied. Resumes are launched first. The call waits for all members and returns one aggregate report. AgentSwarm must be the only tool call in its assistant response.
