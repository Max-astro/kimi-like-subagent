Delegate one bounded task to an isolated subagent session. Valid profile names are listed in the current system prompt. `prompt` must work from zero conversation context: include the deliverable, known paths and context, evidence and write scope, acceptance checks, and stop condition. Ask the child to return completed work, checks, blockers, and remaining work if assumptions fail or required work exceeds that scope. Use background execution only when you retain useful non-conflicting work you can advance now, not just review after completion; continue that work after launch. Completion is delivered automatically, so do not poll or sleep. With no independent work, prefer direct execution or foreground delegation when a bounded handoff provides worthwhile context isolation. Foreground execution blocks the next parent turn until the tool batch returns. Resuming keeps the original profile and model; supply intervening changes and updated scope when continuing.
