Delegate one bounded task to an isolated subagent session. Valid profile names are listed in the current system prompt. `prompt` must be complete enough to work from zero conversation context. Use foreground execution when your next step depends on the result. Use background execution only while useful independent work remains; completion is delivered automatically, so do not poll or sleep. Resuming keeps the original profile and model.
